name: Deploy to Server

on: 
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with: 
          host: 168.110.223.236
          username: bagas
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: /srv/bagas-app/deploy.sh
